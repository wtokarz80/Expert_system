package models;

import java.util.List;

public class MultipleValue extends Value {
    public MultipleValue(List<String> inputPatterns, boolean selectionType) {
        super(inputPatterns, selectionType);
    }
}
